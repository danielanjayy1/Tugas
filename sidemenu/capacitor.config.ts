import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sidemenu-daniel',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
